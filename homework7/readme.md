##Домашнее задание №7 (Java / Обобщения, коллекции, перечисления, исключения / Зоопарк)

###Before

Создать новый проект на gitlab.com или github.com

Скопировать в созданный проект файлы и папки из репозитория https://gitlab.com/lanit-at-school/helpdesk-ui-test.git 

Ничего не меняя в скопированных файлах, выполнить коммит в ветку master. Выполнить push на удаленный репозиторий.

Из текущего состояния создать ветку develop.

###Задание

В классе _**HelpdeskUITest**_ реализовать Шаги в тестовом методе.

Шаги:

    -Открыть страницу https://at-sandbox.workbench.lanit.ru/
    -Создать тикет
    -Выполнить логин (login: admin, pass: adminat)
    -Через поиск найти созданный тикет и убедиться, что данные соответствуют введенным в п. 2.


Класс _**Ticket**_ пакета **models** реализуем по аналогии из домашнего задания 6, с набором полей, необходимым для заполнения формы создания тикета. 

В пакете pages находятся классы отвечающие за работу со страницами:

    -AbstractPage - абстрактный класс описывающий базовый шаблон для абсолютно всех страниц, с которыми вы будете работать.
    -SendboxBasePage - базовый класс для конкретного сайта, в нем описываются элементы присутствующие на всех страницах этого сайта, например меню.
    -MainPage - первая страница на которую мы попадаем при заходе на сайт или прохождении тестов. Как и все остальные страницы описывает элементы конкретной страницы и реализует методы взаимодействия с ними.

В пакете **elements** можно располагать стандартные элементы UI сайта, которые часто встречаются на страницах и/или требуют специальной логики работы с собой (меню, таблицы, карты и тд.).

Заполнить конфигурационные файлы **config.properties** и **user.properties**

Реализовать недостающие поля и методы классов (помечены **todo**)

Успешно провести проверку соответствия вводимых данных и содержания созданного тикета

Выполнить коммит в **develop** и отправить ветку на удаленный репозиторий.

Сделать **merge request** (_pull request_) ветки **develop** в ветку **master**. Убедиться, что изменения (_diff_) соответствуют проделанной работе.

###Требования

Использовать версию Java - 8, если преподаватель не указал иное. 

Тестовые данные для создания тикета хранятся в объекте класса _**Ticket**_ и заполняются методом **buildNewTicket()** класса _**HelpdeskUITest**_

Тикет должен создаваться методом **createTicket** класса _**CreateTicketPage**_ и принимать как входной параметр объект класса _**Ticket**_

Авторизация пользователя должна производится методом **login** класса _**LoginPage**_ и получать данные авторизации из конфигурационного файла **user.properties**

Методы класса _**TicketPage**_ должны возвращать **значения** полей тикета для проверки их в классе _**HelpdeskUITest**_

Не использовать сторонние библиотеки, кроме **JUnit** и **selenium**.

Тесты должны проходить без ошибок.

###Формат проверок 

1. Выполнить работу в проекте с домашним заданием на gitlab.com (разрешается и github.com).

2. Каждое домашнее задание должно быть выполнено в соответствующей отдельной ветке, например для текущей задачи **homework7**.

3. Готовую работу требуется отправить в систему автопроверки ДЗ: https://autoschool.rnd.lanit.ru/

4. В проекте на gitlab.com (github.com) должен быть создан **merge request** (_pull request_) для слияния ветки домашнего задания (например homework7) с основной веткой (например **main** или **master**).